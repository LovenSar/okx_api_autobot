# 快速开始 - 高频交易配置 ⚡

## 🎯 当前配置一览

```
📊 数据更新: 每 0.5 秒（每秒2次请求）
🤖 AI决策:   每 1 秒
🛡️  交易保护: 最小间隔 30 秒
🌐 Web刷新:  每 1 秒
```

---

## 🚀 立即启动

```bash
# 1. 激活虚拟环境
source venv/bin/activate  # Mac/Linux
.\venv\Scripts\activate   # Windows

# 2. 启动服务
python web_server.py

# 3. 打开浏览器
http://localhost:8080
```

---

## ⚙️ 快速调整

### 修改交易间隔保护（推荐先调整这个）

**文件**: `deepseekok2.py` **第68行**

```python
MIN_TRADE_INTERVAL = 30  # 改这个数字

# 建议值：
# 10  = 激进（10秒间隔）
# 30  = 默认（30秒间隔）
# 60  = 保守（1分钟间隔）
# 300 = 超保守（5分钟间隔）
```

### 修改AI决策频率

**文件**: `deepseekok2.py` **第1040行**

```python
time.sleep(1)  # 改这个数字

# 建议值：
# 1  = 每秒1次（当前）
# 5  = 每5秒1次（推荐）
# 10 = 每10秒1次（稳健）
```

### 修改数据更新频率

**文件**: `web_server.py` **第193行**

```python
time.sleep(0.5)  # 改这个数字

# 建议值：
# 0.5 = 每秒2次（当前）
# 1   = 每秒1次（推荐）
# 2   = 每2秒1次（保守）
```

---

## 📊 运行状态监控

启动后你会看到：

```
=============================================================
🚀 启动BTC交易机器人Web监控...
=============================================================

📊 启动实时数据更新线程（每秒2次，间隔0.5秒）...

=============================================================
🌐 Web管理界面启动成功！
📊 访问地址: http://localhost:8080
⏰ AI决策频率: 每1秒分析一次
📈 数据更新: 每0.5秒刷新（每秒2次）
🌐 Web界面: 每1秒自动刷新
🛡️  交易间隔: 最少间隔 30 秒
=============================================================
```

---

## 🛡️ 安全保护机制

即使每秒都决策，也有多重保护：

1. ⏰ **时间间隔**: 距上次交易必须 ≥ 30秒
2. 💪 **信心过滤**: LOW信心不执行
3. 🔄 **反转保护**: 非HIGH信心不反转
4. 🔁 **信号去重**: 近期重复信号跳过
5. 💰 **保证金检查**: 余额不足跳过

---

## 📈 预期行为

### 每分钟

- 数据请求：**120次**（每秒2次）
- AI决策：**60次**（每秒1次）
- 实际交易：**最多2次**（受30秒间隔限制）

### 实际交易频率

- 理论最大：2次/分钟
- 实际平均：0.5-1次/分钟（多数决策不会交易）
- 关键：**有决策不等于有交易**

---

## ⚠️ 重要提示

### 首次使用

```python
# 1. 确保开启测试模式
TRADE_CONFIG = {
    'test_mode': True,  # 改为 True
    'amount': 0.001,    # 用小金额测试
}

# 2. 运行观察1小时

# 3. 确认策略正常后再改为实盘
'test_mode': False
```

### API限流

如果遇到 `HTTP 429` 错误：

```python
# 方案1：降低数据更新频率
time.sleep(1)  # web_server.py 第193行

# 方案2：降低AI决策频率  
time.sleep(5)  # deepseekok2.py 第1040行
```

---

## 🔧 常见问题

### Q: 为什么一直决策但不交易？

A: 正常现象！检查日志中的保护机制提示：
```
🔒 距上次交易仅 15 秒，需等待 15 秒后才能交易
🔒 非高信心反转信号，保持现有long仓
⚠️ 低信心信号，跳过执行
```

### Q: 如何调整更激进？

A: 修改交易间隔：
```python
MIN_TRADE_INTERVAL = 10  # 从30改为10秒
```

### Q: 如何调整更保守？

A: 增加所有间隔：
```python
MIN_TRADE_INTERVAL = 60   # 交易间隔改为60秒
time.sleep(10)            # AI决策改为10秒
time.sleep(2)             # 数据更新改为2秒
```

---

## 📝 修改后记得重启

```bash
# 修改配置文件后
# 按 Ctrl+C 停止
# 重新运行
python web_server.py

# 或Docker用户
docker-compose restart
```

---

## 📚 详细文档

- **完整配置说明**: `HIGH_FREQUENCY_CONFIG.md`
- **实时更新说明**: `REALTIME_UPDATE_GUIDE.md`
- **Docker部署**: `DOCKER_GUIDE.md`
- **环境配置**: `ENV_CONFIG.md`

---

**准备好了？开始交易吧！** 🚀📈

