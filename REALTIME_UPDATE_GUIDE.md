# 实时数据更新配置说明

## 📊 配置概述

本次修改实现了两个独立的更新频率：

| 功能 | 更新频率 | 说明 |
|------|---------|------|
| **实时数据刷新** | 每 1 秒 | 更新价格、持仓、余额等实时数据 |
| **AI决策交易** | 每 60 秒 | 执行完整的AI分析和交易决策 |
| **Web界面刷新** | 每 1 秒 | 前端自动拉取最新数据 |

---

## 🔄 修改内容

### 1. Web界面刷新频率

**文件**: `static/js/app.js`

**修改**:
```javascript
// 从 10秒 改为 1秒
setInterval(updateData, 1000);  // 每1秒刷新
```

**效果**: 用户界面每秒自动更新，显示最新价格和持仓

---

### 2. 实时数据更新函数

**文件**: `deepseekok2.py`

**新增函数**: `update_realtime_data()`

```python
def update_realtime_data():
    """实时更新价格和持仓数据（轻量级，不做AI决策）"""
    # 只获取最新价格
    # 更新持仓信息
    # 更新账户余额
    # 不做AI分析，速度快
```

**特点**:
- ✅ 轻量级：只获取必要数据
- ✅ 快速：不进行技术分析和AI决策
- ✅ 高效：适合高频更新

---

### 3. 后台更新线程

**文件**: `web_server.py`

**新增函数**: `run_realtime_update()`

```python
def run_realtime_update():
    """后台线程：每秒更新实时数据"""
    while True:
        deepseekok2.update_realtime_data()
        time.sleep(1)  # 每秒更新
```

**启动方式**:
```python
# 启动两个独立线程
realtime_thread = threading.Thread(target=run_realtime_update, daemon=True)
bot_thread = threading.Thread(target=run_trading_bot, daemon=True)
```

---

### 4. 交易决策频率

**文件**: `deepseekok2.py`

**修改**:
```python
# 主循环：每60秒执行一次完整决策
while True:
    trading_bot()  # AI分析 + 技术指标 + 交易执行
    time.sleep(60)  # 等待60秒
```

**移除**: 15分钟整点等待逻辑

---

## 🚀 启动方式

### 方法1: Web界面（推荐）

```bash
# 激活虚拟环境
source venv/bin/activate  # Linux/Mac
.\venv\Scripts\activate   # Windows

# 启动Web服务器
python web_server.py
```

访问: http://localhost:8080

**启动后会有两个线程同时运行**:
1. 🔄 实时数据线程：每1秒更新
2. 🤖 AI决策线程：每60秒分析

---

### 方法2: Docker部署

```bash
# 重启容器应用新配置
docker-compose restart
```

---

## 📈 数据流程

```
┌─────────────────────────────────────────────┐
│  实时数据线程（每1秒）                        │
│  • 获取最新价格                              │
│  • 更新持仓信息                              │
│  • 更新账户余额                              │
│  ❌ 不做AI分析                               │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Web数据存储 (web_data)                     │
│  • current_price                            │
│  • current_position                         │
│  • account_info                             │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  Web界面（每1秒刷新）                        │
│  显示实时价格和持仓变化                      │
└─────────────────────────────────────────────┘


┌─────────────────────────────────────────────┐
│  AI决策线程（每60秒）                        │
│  • 获取完整K线数据                           │
│  • 计算技术指标                              │
│  • 调用AI模型分析                            │
│  • 执行交易决策                              │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  交易执行                                    │
│  • BUY / SELL / HOLD                       │
│  • 记录交易历史                              │
└─────────────────────────────────────────────┘
```

---

## ⚙️ 自定义配置

### 修改实时更新频率

编辑 `web_server.py` 第193行：

```python
time.sleep(1)   # 改为其他值
# 例如：
time.sleep(2)   # 每2秒更新
time.sleep(0.5) # 每0.5秒更新（更快）
```

⚠️ **注意**: 频率过高可能导致API限流

---

### 修改AI决策频率

编辑 `deepseekok2.py` 第1002行：

```python
time.sleep(60)  # 改为其他值
# 例如：
time.sleep(30)   # 每30秒决策（更激进）
time.sleep(120)  # 每120秒决策（更保守）
time.sleep(300)  # 每5分钟决策
```

---

### 修改Web界面刷新

编辑 `static/js/app.js` 第11行：

```javascript
setInterval(updateData, 1000);
// 改为：
setInterval(updateData, 500);   // 0.5秒（更快）
setInterval(updateData, 2000);  // 2秒（更慢）
```

---

## 🔍 监控与调试

### 查看运行日志

```bash
# 直接运行时
python web_server.py

# Docker运行时
docker-compose logs -f
```

### 日志输出示例

```
📊 启动实时数据更新线程（每1秒更新）...
⏰ 执行频率: 每60秒（1分钟）执行一次AI决策
📈 数据刷新: 每1秒更新价格和持仓
🌐 Web界面: 每1秒自动刷新

=============================================================
执行时间: 2025-10-27 14:30:00
=============================================================
BTC当前价格: $70,250.50
数据周期: 15m
价格变化: +0.32%
⏳ 正在调用AI API...
```

---

## 📊 性能影响

### API调用频率对比

| 操作 | 之前 | 现在 | 变化 |
|-----|------|------|------|
| 价格获取 | 15分钟 | 1秒 | ↑ 900倍 |
| 持仓查询 | 15分钟 | 1秒 | ↑ 900倍 |
| AI决策 | 15分钟 | 60秒 | ↑ 15倍 |
| Web刷新 | 10秒 | 1秒 | ↑ 10倍 |

### 建议

1. **API限流风险**: OKX有API调用频率限制
   - 公共接口: 20次/2秒
   - 私有接口: 10次/2秒
   
2. **优化方案**:
   - 实时更新间隔不要低于1秒
   - 监控API调用失败情况
   - 如遇限流，增加间隔时间

3. **资源消耗**:
   - CPU: 略微增加（轻量级更新）
   - 网络: 增加（更频繁的API调用）
   - 内存: 无明显变化

---

## 🎯 推荐配置

### 日间交易（激进）
```python
# 实时数据: 1秒
time.sleep(1)

# AI决策: 30秒
time.sleep(30)

# Web刷新: 1秒
setInterval(updateData, 1000)
```

### 稳健交易（默认）
```python
# 实时数据: 1秒
time.sleep(1)

# AI决策: 60秒
time.sleep(60)

# Web刷新: 1秒
setInterval(updateData, 1000)
```

### 保守交易
```python
# 实时数据: 2秒
time.sleep(2)

# AI决策: 300秒（5分钟）
time.sleep(300)

# Web刷新: 2秒
setInterval(updateData, 2000)
```

---

## ⚠️ 注意事项

1. **API限流**: 注意OKX的API调用限制
2. **网络稳定性**: 高频更新对网络要求更高
3. **交易成本**: AI决策频率影响交易次数
4. **止损止盈**: 更频繁的决策可能触发更多交易

---

## 🔧 故障排查

### 1. 实时数据不更新

**检查**:
```bash
# 查看日志中是否有错误
grep "实时数据更新失败" logs.txt
```

**解决**:
- 检查网络连接
- 验证API密钥
- 检查API限流

### 2. AI决策失败

**检查**:
```bash
# 查看AI调用日志
grep "AI分析失败" logs.txt
```

**解决**:
- 检查AI API余额
- 验证API密钥
- 增加决策间隔

### 3. Web界面卡顿

**原因**: 1秒刷新可能过快

**解决**:
```javascript
// 改为2秒刷新
setInterval(updateData, 2000);
```

---

## 📝 总结

### 优势
- ✅ 实时看到价格变化
- ✅ 更快的决策响应
- ✅ 更好的用户体验
- ✅ 独立的更新逻辑

### 权衡
- ⚠️ 更多API调用
- ⚠️ 可能遇到限流
- ⚠️ 更频繁的交易
- ⚠️ 需要稳定网络

---

**配置完成！祝交易顺利！** 📈✨

